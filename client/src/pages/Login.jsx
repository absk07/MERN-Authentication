import React from 'react';
import AuthForm from '../components/AuthForm';

const Login = () => {
    return (
        <AuthForm
            headerText="Login"
            buttonText="Login"
        />
    );
}

export default Login;